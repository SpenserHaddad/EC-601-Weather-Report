# EC-601-Weather-Report

An easy-to-use web interface for viewing the weather


## Installation
    Install Python 3.6
    Install dependencies using pip install -r requirements.txt

    Run with
        python server.py